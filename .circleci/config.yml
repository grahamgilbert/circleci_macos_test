version: 2
jobs:
  build-and-test:
    macos:
      xcode: "10.1.0"
    shell: /bin/bash --login -o pipefail
    steps:
      - checkout
      - run: pip install awscli

workflows:
  version: 2
  build-test:
    jobs:
      - build-and-test
